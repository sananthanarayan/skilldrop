# Every change requires review from the repo owner.
# Takes effect once the `main` ruleset enables "Require review from Code Owners"
# (GitHub → Settings → Rules → Rulesets). Until then it is inert.
* @sananthanarayan
