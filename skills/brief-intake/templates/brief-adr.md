# Brief: ADR

> Target downstream skill: `adr-generator`

## Decision
{One-sentence noun phrase — the decision, not the question} [explicit|implied|inferred|missing]
> "{verbatim snippet from source}"

## Context / forcing function
{Why is a decision needed *now*? What changed?} [explicit|implied|inferred|missing]
> "{verbatim snippet}"

## Decision drivers
- {Driver 1 — constraint, requirement, or non-negotiable} [explicit|implied|inferred|missing]
- {Driver 2}
- {Driver 3}

## Options considered
- **Option A** — {one-line description} [explicit|implied|inferred|missing]
- **Option B** — {one-line description}
- **Option C** — {one-line description, or omit if only two were considered}

## Chosen option
**{Option name}** — because {one-sentence rationale tied to a driver above} [explicit|implied|inferred|missing]

## Consequences / trade-offs
- ✅ {what becomes easier} [explicit|implied|inferred|missing]
- ⚠️ {acceptable trade-off}
- ❌ {what becomes harder or constrains future choices}

## Format hint
{`MADR` (default) | `Nygard` (if source frames it as a simple decision)}

## Numbering hint
{ADR directory mentioned in source, e.g. `docs/adr/` — or `unknown` so adr-generator asks}

---

## 🚩 Missing for downstream
- {field: why it matters to adr-generator}
- {e.g. "Only 1 option captured — adr-generator requires ≥2 considered options"}
