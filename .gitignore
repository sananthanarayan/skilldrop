# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp
*~

# Python (for the figma-diagrams skill scripts)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/

# Local secrets — never commit
.env
.env.local
*.figmatoken

# Generated catalogue site (RFC-0011) — built by CI, never committed
build/

# Generated per-pack Claude plugins (RFC-0027) — built by build_marketplace.py --dist,
# published to the plugins branch; never committed to main
build-plugins/

# Test renders / scratch output
out/
renders/
*.png
*.svg
*.pdf
!skills/**/examples/**/*.png
!skills/**/examples/**/*.svg
# …and the committed site assets. og.png is rendered once from assets/og.svg with
# rsvg-convert and committed, so build_site.py stays stdlib-only (RFC-0011).
!assets/*.svg
!assets/*.png

# Claude local state
.claude/settings.local.json

# agent tooling scratch (attachments, session state) — never repo content
.context/
